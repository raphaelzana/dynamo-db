spring.application.name=dynamo-test

# DynamoDB settings
app.dynamodb.region=us-east-1
# Optional: point to LocalStack or a custom endpoint
# app.dynamodb.endpoint=http://localhost:4566
# Table name to use/create
app.dynamodb.tableName=ddb-testable
